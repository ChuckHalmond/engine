export class RenderPipeline {

    // configuration:
    // - antialiasing
    
    // culling
    // baking (creating intermediate textures (lightMap, normalMap))
    // batching (combining attributes, uniforms, etc.)

    // lighting ?

    // stencil testing
    // depth testing
    // blending (enablabe) -> src <-> dest
}