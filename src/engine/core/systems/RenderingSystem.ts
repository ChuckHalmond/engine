import { System } from "../general/System";
import { ComponentDesc } from "../general/Component";

export class RenderingSystem extends System {
    /*components: Array<RenderingComponent<ComponentDesc>>;

    constructor() {
        super();
        this.components = [];
    }

    public iterate(dT: number): void {
        for (const component of this.components) {
            component.render();
        }
    }

    public register(component: RenderingComponent<ComponentDesc>) {
        this.components.push(component);
    }*/
}